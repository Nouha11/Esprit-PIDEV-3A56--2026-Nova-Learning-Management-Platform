{# Login History Widget Component - For User Dashboards #}
{% set recentLogins = recentLogins|default([]) %}
{% set limit = limit|default(5) %}

<div class="card border-0 shadow-sm h-100">
    <div class="card-header border-bottom">
        <div class="d-flex justify-content-between align-items-center">
            <h5 class="mb-0">
                <i class="bi bi-clock-history text-primary"></i>
                Recent Login Activity
            </h5>
            {% if recentLogins|length > 0 %}
                <span class="badge bg-primary">{{ recentLogins|length }}</span>
            {% endif %}
        </div>
    </div>
    <div class="card-body">
        {% if recentLogins is empty %}
            <div class="text-center py-4">
                <i class="bi bi-clock-history fs-1 text-muted"></i>
                <p class="text-muted mt-2 mb-0">No login history yet</p>
            </div>
        {% else %}
            <div class="list-group list-group-flush">
                {% for login in recentLogins|slice(0, limit) %}
                    <div class="list-group-item login-history-item px-0 {% if not loop.last %}border-bottom{% endif %}">
                        <div class="d-flex align-items-start">
                            <div class="flex-shrink-0 me-3">
                                {% if login.status == 'success' %}
                                    <div class="login-icon-success rounded-circle p-2">
                                        <i class="bi bi-check-circle-fill text-success"></i>
                                    </div>
                                {% elseif login.status == 'failed' %}
                                    <div class="login-icon-danger rounded-circle p-2">
                                        <i class="bi bi-x-circle-fill text-danger"></i>
                                    </div>
                                {% else %}
                                    <div class="login-icon-warning rounded-circle p-2">
                                        <i class="bi bi-shield-exclamation text-warning"></i>
                                    </div>
                                {% endif %}
                            </div>
                            
                            <div class="flex-grow-1">
                                <div class="d-flex justify-content-between align-items-start mb-1">
                                    <div>
                                        {% if login.status == 'success' %}
                                            <span class="badge login-badge-success small">Success</span>
                                        {% elseif login.status == 'failed' %}
                                            <span class="badge login-badge-danger small">Failed</span>
                                        {% else %}
                                            <span class="badge login-badge-warning small">Blocked</span>
                                        {% endif %}
                                        
                                        {% if login.is2faUsed %}
                                            <span class="badge login-badge-primary small ms-1">
                                                <i class="bi bi-shield-check"></i> 2FA
                                            </span>
                                        {% endif %}
                                    </div>
                                    <small class="text-muted">
                                        {{ login.createdAt|date('M d, H:i') }}
                                    </small>
                                </div>
                                
                                <div class="small text-muted">
                                    {% if login.browser %}
                                        <i class="bi bi-browser-chrome"></i> {{ login.browser }}
                                    {% endif %}
                                    {% if login.platform %}
                                        • {{ login.platform }}
                                    {% endif %}
                                    {% if login.device %}
                                        • {{ login.device }}
                                    {% endif %}
                                </div>
                                
                                {% if login.ipAddress %}
                                    <div class="small text-muted mt-1">
                                        <i class="bi bi-geo-alt"></i> {{ login.ipAddress }}
                                        {% if login.location %}
                                            ({{ login.location }})
                                        {% endif %}
                                    </div>
                                {% endif %}
                                
                                {% if login.failureReason %}
                                    <div class="alert alert-danger alert-sm mt-2 mb-0 py-1 px-2">
                                        <small><i class="bi bi-exclamation-triangle"></i> {{ login.failureReason }}</small>
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            
            {% if recentLogins|length > limit %}
                <div class="text-center mt-3">
                    <a href="#" class="btn btn-sm btn-outline-primary">
                        View All History
                    </a>
                </div>
            {% endif %}
        {% endif %}
    </div>
</div>

<style>
/* Light theme styles */
.login-history-item {
    background-color: transparent;
}

.login-icon-success {
    background-color: rgba(25, 135, 84, 0.1);
}

.login-icon-danger {
    background-color: rgba(220, 53, 69, 0.1);
}

.login-icon-warning {
    background-color: rgba(255, 193, 7, 0.1);
}

.login-badge-success {
    background-color: rgba(25, 135, 84, 0.1);
    color: #198754;
}

.login-badge-danger {
    background-color: rgba(220, 53, 69, 0.1);
    color: #dc3545;
}

.login-badge-warning {
    background-color: rgba(255, 193, 7, 0.1);
    color: #ffc107;
}

.login-badge-primary {
    background-color: rgba(13, 110, 253, 0.1);
    color: #0d6efd;
}

/* Dark theme styles */
[data-bs-theme="dark"] .login-icon-success {
    background-color: rgba(25, 135, 84, 0.2);
}

[data-bs-theme="dark"] .login-icon-danger {
    background-color: rgba(220, 53, 69, 0.2);
}

[data-bs-theme="dark"] .login-icon-warning {
    background-color: rgba(255, 193, 7, 0.2);
}

[data-bs-theme="dark"] .login-badge-success {
    background-color: rgba(25, 135, 84, 0.2);
    color: #75b798;
}

[data-bs-theme="dark"] .login-badge-danger {
    background-color: rgba(220, 53, 69, 0.2);
    color: #ea868f;
}

[data-bs-theme="dark"] .login-badge-warning {
    background-color: rgba(255, 193, 7, 0.2);
    color: #ffda6a;
}

[data-bs-theme="dark"] .login-badge-primary {
    background-color: rgba(13, 110, 253, 0.2);
    color: #6ea8fe;
}

[data-bs-theme="dark"] .login-history-item {
    border-color: rgba(255, 255, 255, 0.1) !important;
}

.alert-sm {
    font-size: 0.875rem;
}

.list-group-item {
    padding-top: 0.75rem;
    padding-bottom: 0.75rem;
}
</style>
