framework:
    messenger:
        failure_transport: failed

        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            # Async transport using Doctrine (Requirements: 23.1)
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                # Retry strategy: 3 retries with exponential backoff (Requirements: 23.4)
                retry_strategy:
                    max_retries: 3
                    multiplier: 2
                    delay: 1000 # Initial delay in milliseconds
                    max_delay: 0 # No maximum delay
                options:
                    # Auto setup creates the messenger_messages table if it doesn't exist
                    auto_setup: true
            failed: 'doctrine://default?queue_name=failed'
            # sync: 'sync://'

        default_bus: messenger.bus.default

        buses:
            messenger.bus.default: []

        routing:
            # Send emails asynchronously for better performance (Requirements: 23.1)
            Symfony\Component\Mailer\Messenger\SendEmailMessage: async
            Symfony\Component\Notifier\Message\ChatMessage: async
            Symfony\Component\Notifier\Message\SmsMessage: async

            # Route your messages to the transports
            # 'App\Message\YourMessage': async
